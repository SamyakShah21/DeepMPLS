#ifndef OUTCOME_H
#define OUTCOME_H

namespace utils {
    //defining an enum (enumerate) object to print the output in the final file.
    enum outcome_t { YES, NO, MAYBE };    
}

#endif //OUTCOME_H 
